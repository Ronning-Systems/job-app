fastapi>=0.109.1
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pydantic==2.5.3
httpx>=0.27.0
beautifulsoup4==4.12.3
python-multipart>=0.0.18
python-dotenv==1.0.0
pypdf>=3.17.0
python-docx==0.8.11
pdfplumber==0.10.3
psycopg2-binary
PyJWT[cryptography]>=2.8.0
alembic==1.13.1

# Test dependencies (used by backend/tests/ + sidecars/*/tests/).
# pytest + httpx are already in the runtime deps; pytest is added
# here so the test suite has a single place to declare its needs.
pytest==8.3.3